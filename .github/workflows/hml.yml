name: paimon CI hml
on:
  push:
    branches: 
      - hml

jobs:
  tests:
    runs-on: self-hosted
    steps:
      - name: aaaaa
        uses: actions/checkout@v4.1.2
      - run: ls
    
  build:
    needs: tests
    runs-on: self-hosted
    steps:
      - name: bbbb
        uses: actions/checkout@v4.1.2
      - run: ls