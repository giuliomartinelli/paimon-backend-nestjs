name: paimon CI hml
on:
  push:
    branches: 
      - hml

jobs:
  unit-tests:
    runs-on: self-hosted
    steps:
      - name: aaaaa
        uses: actions/checkout@v4.1.2
      - run: |
          touch .env
          echo "${{ secrets.PROD_PAIMON_NESTJS }}" > .env
          ls -la 
    
  build:
    needs: tests
    runs-on: self-hosted
    steps:
      - name: bbbb
        uses: actions/checkout@v4.1.2
      - run: |
          ls -la